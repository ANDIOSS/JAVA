파일의 크기 순서
int -> long -> double  
작은 범위에서 큰 범위로는 대입할 수 있다
-이것을 묵시적 형변환 또는 자동 형변환이라고 부른다.
큰 범위에서 작은 범위로 변환 될때에는 몇가지 문제가 발생한다
- 소수점을 버린다 , 오버플로우가 생긴다
Ex) int 1_000_000_001 ~ int 1_000_000_010 을 더 하려고 하면 결과값이 -가 된다
이것이 오버플로우다 int의 최대저장값은 21억이 조금 넘는데 위에 값을 다 더하면 21억이 넘기 때문에 -로 가는 것이다
double x = 12.123123 인데 이 값을 int에 대입하면 뒤에 소숫점은 버리고 12 로 바뀐다 물론 여기서 double x값을 호출하면 값은 그대로 있다

형변환, 연산
같은 타입은 같은 타입의 결과가 나온다
서로 다른 타입은 범위가 큰 것으로 결과가 나온다
Ex) double / int  = double 결과가 나온다는 것

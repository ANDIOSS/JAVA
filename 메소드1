반환타입 메소드 이름(매개변수타입 매개변수, 매개변수타입 매개변수,...) {
수행문;
수행문;
수행문;
}

반환타입이란?
- 메소드 실행 후 최종적으로 획득되는 값의 타입이다.
- 반환타입의 종류
 void : 메소드 실행 후 획득되는 값이 없다. 메소드가 값을 반환하지 않는다
         * 메소드가 추가/변경/삭제 작업을 수행하는 경우
         * 메소드가 출력작업을 수행하는 경우
 그 외 : 메소드 실행 후 획득되는 값이 있다. 메소드가 값을 반환한다.
        메소드 선언부의 반환타입 자리에 해당 메소드가 반환하는 값의 타입을 적어야한다.   
        * 메소드가 조회 작업을 수행하는 경우
        * 메소드가 계산 작업을 수행하고, 계산 결과를 제공하는 경우
        * 메소드가 입력값에 대한 변환작업을 수행하고 반환 결과를 제공하는 경우
void를 아닌 다른 메소드들은 최종적으로 값을 돌려주는 return이 있어야 한다.
메소드의 반환타입을 지정해야한다. EX) int plus() double plus()

매개변수(Parameter) 특정한 작업을 수행할 때 필요한 값을 파라미터라고 부른다.
메소드의 수행문을 실행하는데 필요한 값을 전달받기 위한 변수다.
메소드는 매개변수를 0개 이상 정의할 수 있다.
메소드의 매개변수가 여러 개 인 경우, 각 매개변수는 ","로 구분한다.
매개변수가 정의된 메소드를 실행할 때는 반드시 매개변수에 맞게 값이 전달되어야 한다.
* 매개변수의 갯수와 파라미터 값의 갯수가 일치해야 한다.
* 매개변수의 타입 순서와 파라미터값의 타입 순서가 일치해야 한다.

int plus(int x, int y) {
}

plus (1 , 20)   // 1과 20을 파라미터값 혹은 인자값이라고 부른다.
plus (10)       //오류 갯수가 일치하지 않음 
plus (3.14)     //오류 타입이 일치하지 않음
plus()          //오류 값이 없음

int getPoint(int orderPrice, double depositRate) {
}
get Point(300000, 0.15);
get Point(0.15, 30000);    //오류
